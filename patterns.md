# [Client Name] — Patterns

> Confirmed patterns from prototype iterations. Grows per feature.
> Check this before every new Constraint Map — what worked before likely works again.

## UI Patterns

*Empty until first feature ships. Add entries after Knowledge Capture.*

<!--
Example entry:
### Evaluatie-tabel pattern
- **Context:** Decision support with multiple criteria
- **Pattern:** Three statuses (positive / negative / park), not binary
- **Confirmed in:** [Feature name]
- **Why it works:** Users rarely have a binary yes/no — parking reduces decision anxiety
-->

## Interaction Patterns

<!--
Example entry:
### Preset-profielen voor bulk configuratie
- **Context:** 10+ items to configure individually
- **Pattern:** Offer preset templates as starting point, then customize
- **Confirmed in:** [Feature name]
- **Why it works:** Reduces 60+ clicks to 3 clicks + exceptions
-->

## Anti-Patterns (Don't Repeat These)

<!--
Example entry:
### Modal per item voor bulk operaties
- **Context:** Tried in [feature name]
- **What happened:** Tedious, no comparison possible across items
- **Instead:** Use table with inline editing
-->
